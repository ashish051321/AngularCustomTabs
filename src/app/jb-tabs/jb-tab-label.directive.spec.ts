import { JbTabLabelDirective } from './jb-tab-label.directive';

describe('JbTabLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new JbTabLabelDirective();
    expect(directive).toBeTruthy();
  });
});
